# Example configuration only. Never commit real secrets or private keys.
DATABASE_URL=
AI_SERVICE_URL=
BLOCKCHAIN_RPC_URL=
CONTRACT_ADDRESS=
ISSUER_PRIVATE_KEY=
JWT_SECRET=
NEXT_PUBLIC_API_URL=
# A2.1: origen HTTPS publico y estable del backend NestJS (por ejemplo el
# Web Service de Render), usado unicamente para provisionar did:web de
# holders. Opcional: si se deja vacia, el registro/emision siguen
# funcionando con User.did=null (sin provisioning), como antes de A2.1.
# Debe ser https://<host>[:puerto] sin path/query/fragment/usuario -- nunca
# localhost ni una IP. Nunca commitear el valor real de produccion.
PUBLIC_DID_BASE_URL=
